---
title: 📦 包管理工具大乱斗：终极速查表
date: 2021-07-23
categories:
  - 效率工具
tags:
  - Tools
---


# 📦 包管理工具大乱斗：终极速查表

> npm 是元老，Yarn 是改良者，pnpm 是极简主义者，Bun 是... 那个跑得飞快的年轻人。

还在记不住不同工具的命令？别担心，脑容量应该留给更有用的东西（比如今晚吃什么）。这里有一份跨语言、跨工具的翻译指南。

## 🚀 常用命令对照表

| 动作 | npm (经典款) | yarn (经典款 v1) | pnpm (省空间款) | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **安装依赖** | `npm install` | `yarn` | `pnpm i` | 拿到新项目的第一件事 |
| **加个包** | `npm i package` | `yarn add package` | `pnpm add package` | 默认都是生产依赖 |
| **加个开发包** | `npm i -D package` | `yarn add -D package` | `pnpm add -D package` | 比如 TS, ESLint |
| **移除包** | `npm uninstall pkg` | `yarn remove pkg` | `pnpm remove pkg` | 分手快乐 |
| **运行脚本** | `npm run dev` | `yarn dev` | `pnpm dev` | `yarn` 和 `pnpm` 可以省掉 `run` |
| **交互式更新** | - | `yarn upgrade-interactive --latest` | `pnpm up -i --latest` | **超好用！** 像点菜一样选要升级的包 |

## 🧐 "为什么我有这个包？"

你的 `node_modules` 还是那么重，你想知道是哪个依赖偷偷引入了 `is-odd` 这种奇怪的包？

- **npm**: `npm list package-name`
- **yarn**: `yarn why package-name` (这命令名字起得真好)
- **pnpm**: `pnpm why package-name`

## 🧹 大扫除

有时候依赖坏了，最快的修复方法就是... 重启大法好（删除重装）。

```bash
# 这种时候就不要优雅了，直接粗暴点
rm -rf node_modules
# 然后再选你的武器
pnpm install
```

---

*选一个你喜欢的工具，然后坚持用它。除非你的 Tech Lead 强迫你换。*
